/**
 * `@debug` — the developer overlay and runtime metrics. A consumer: reads the
 * simulation projection and displays FPS, tick rate, seed, sim time, current
 * state, and (later) topology graph, active cascades, and event history.
 */
export * from './metrics/metrics-collector';
export * from './debug-overlay';
