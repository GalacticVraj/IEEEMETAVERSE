import type { ReactElement } from 'react';

/**
 * The heads-up display: load gauges, zone alerts, crisis banner, countdown, the
 * floating advisor callouts. Reads simulation projections; never computes state.
 * Phase-1 placeholder: renders nothing.
 */
export function Hud(): ReactElement | null {
  return null;
}
