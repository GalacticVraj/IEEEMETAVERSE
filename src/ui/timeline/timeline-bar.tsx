import type { ReactElement } from 'react';

/**
 * The run timeline / scrubber. Shows key beats (first overload, cascade,
 * blackout, recovery) from replay markers and enables seeking.
 * Phase-1 placeholder: renders nothing.
 */
export function TimelineBar(): ReactElement | null {
  return null;
}
