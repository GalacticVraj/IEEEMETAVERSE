import type { ReactElement } from 'react';

/**
 * Transport controls for replay: play/pause, speed, step, seek. Drives the
 * `IReplayPlayer`; reacts to `ReplayStarted` / `ReplayFinished`.
 * Phase-1 placeholder: renders nothing.
 */
export function ReplayControls(): ReactElement | null {
  return null;
}
