import type { ReactElement } from 'react';

/**
 * Settings & accessibility panel: quality tier, audio mix, color-blind-safe
 * toggles, keyboard-control reference. Phase-1 placeholder: renders nothing.
 */
export function SettingsPanel(): ReactElement | null {
  return null;
}
